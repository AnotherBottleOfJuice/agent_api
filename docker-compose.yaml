version: '3.8'

services:
  simple_mcp:
    build: ./simple_mcp
    ports:
      - "8015:8000"
    restart: always

  api_agent:
    build: ./agent_api
    ports:
      - "4015:8000"
    depends_on:
      - simple_mcp
    restart: always
      